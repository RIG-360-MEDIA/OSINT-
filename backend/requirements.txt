# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
# Rate limiting (D-06): per-IP/per-user quotas on LLM and admin endpoints.
slowapi==0.1.9

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.2

# Validation
pydantic>=2.10
pydantic-settings>=2.6

# Task queue (sqlalchemy extra required for sqla+postgresql broker)
celery[sqlalchemy]==5.4.0

# HTTP client
httpx==0.27.2
aiohttp==3.10.5

# NLP — entity extraction
spacy==3.7.6
# en_core_web_sm installed via: python -m spacy download en_core_web_sm

# NLP — embeddings (LaBSE)
sentence-transformers==3.1.1

# NLP — language detection
langdetect==1.0.9

# Translation
deep-translator==1.11.4

# HTML extraction
trafilatura==1.12.0

# RSS/HTML parsing
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml==5.3.0

# Groq
groq==0.11.0

# Supabase auth
supabase==2.7.4

# Data processing
python-dateutil==2.9.0

# PDF export (collections)
reportlab==4.2.2

# Tiered scraping
crawl4ai==0.4.248
playwright>=1.47.0

# YouTube transcript extraction (P14 Clip Room)
youtube-transcript-api==1.2.4
# yt-dlp ≥ 2025.1: needed for nsig extraction + JS-runtime (deno) support
# on current YouTube. Older 2024.11.18 falls back to generic n-function
# search and returns "Only images are available" on most videos.
yt-dlp>=2026.03.17
webvtt-py==0.5.1
# SOCKS proxy support for YOUTUBE_PROXY_URL (WARP) — required by requests[socks]
pysocks==1.7.1

# THE NEWSROOM (Phase 2) — local 3-Lens transcription pipeline.
# faster-whisper is the L3 ASR (CPU int8). Default model is `small`
# (~480MB on disk, ~2GB RAM at runtime) — see backend/tasks/newsroom/lens_l3.py
# for the upgrade path to large-v3 once host RAM allows.
# pyphonetics + metaphone power the proper-noun snap step
# (backend/tasks/newsroom/phonetic_snap.py) so transcripts that
# mangle Indian names ("Revant Reddi" -> "Revanth Reddy") still snap
# to the right entity_dictionary row. pyannote.audio is intentionally
# NOT in requirements — the diarise stub kicks in until HF_TOKEN is set.
faster-whisper==1.0.3
pyphonetics==0.5.3
metaphone==0.6

# Newspaper clipping pipeline (P16 Cutting Room)
# PyMuPDF renders the cropped article image; opendataloader-pdf
# does layout-aware article extraction (requires Java at runtime).
PyMuPDF==1.24.10
opendataloader-pdf==0.0.16
importlib_resources==6.4.5

# Sentiment analysis (P17 Signal Room)
vaderSentiment==3.3.2
textblob==0.18.0.post0

# Telegram MTProto user-account client (P17 — read public channels
# without requiring bot membership)
telethon==1.36.0

# Utilities
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
