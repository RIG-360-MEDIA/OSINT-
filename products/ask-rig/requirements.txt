# Ask-RIG runtime deps. Heavy: sentence-transformers pulls torch+transformers
# (~GBs) for query embedding — install last / in the background.
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic>=2.6
python-dotenv>=1.0
openai>=1.30
httpx>=0.27
numpy>=1.26

# per-user app DB (Set 2 — writable, separate from the read-only corpus)
aiosqlite>=0.19
# live-web extraction (Set 3)
trafilatura>=1.8

# query embedding (LaBSE) + cross-encoder reranker (bge-reranker-v2-m3) — heavy
sentence-transformers>=2.7

# dev / test
pytest>=8.0
pytest-asyncio>=0.23
