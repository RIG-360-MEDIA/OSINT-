# Dev-only test + lint dependencies. Installed by Dockerfile.backend in
# the same layer as the runtime requirements so `docker compose run
# rig-backend pytest` works inside the container.
#
# fix/brief-prod-readiness P3.16. Audit found pytest was unavailable in
# the deployed image, so the brief test suite couldn't run inside CI.

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
freezegun==1.5.1
