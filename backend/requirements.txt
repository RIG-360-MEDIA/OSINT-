# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.27  # security: CVE-2024-53981, CVE-2026-24486, CVE-2026-40347, CVE-2026-42561
# Rate limiting (D-06): per-IP/per-user quotas on LLM and admin endpoints.
slowapi==0.1.9

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.2

# Validation
pydantic>=2.10
pydantic-settings>=2.6

# Task queue (sqlalchemy extra required for sqla+postgresql broker)
celery[sqlalchemy]==5.4.0

# HTTP client
httpx==0.27.2
aiohttp==3.13.4  # security: 14 CVEs incl. CVE-2024-52304, CVE-2025-53643, CVE-2026-22815, CVE-2026-34513-34525

# NLP — entity extraction
spacy==3.7.6
# en_core_web_sm installed via: python -m spacy download en_core_web_sm

# NLP — embeddings (LaBSE)
sentence-transformers==3.1.1

# NLP — language detection
langdetect==1.0.9

# Translation
deep-translator==1.11.4

# HTML extraction
trafilatura==1.12.0

# RSS/HTML parsing
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml==6.1.0  # security: CVE-2026-41066

# Groq
groq==0.11.0

# Supabase auth
supabase==2.7.4

# Data processing
python-dateutil==2.9.0

# Clustering — community detection (story layer). igraph's C-backed Louvain splits the
# whole-corpus megablob in tens of MB; networkx Louvain needs GBs (the 12 GiB OOM wall).
# REQUIRED, not optional: cluster_job_7 + story_loader FAIL LOUD (refuse to run) if igraph is
# absent rather than silently falling back to the OOM-prone — and differently-clustering —
# networkx path. Keep both pinned so an image rebuild can never quietly drop them.
igraph==1.0.0
leidenalg==0.12.0

# PDF export (collections)
reportlab==4.2.2

# Tiered scraping
crawl4ai==0.4.248
playwright>=1.47.0

# YouTube transcript extraction (P14 Clip Room)
youtube-transcript-api==1.2.4
yt-dlp==2024.11.18
webvtt-py==0.5.1
# SOCKS proxy support for YOUTUBE_PROXY_URL (WARP) — required by requests[socks]
pysocks==1.7.1

# Newspaper clipping pipeline (P16 Cutting Room)
# PyMuPDF renders pages to images for PP-Structure + clips articles to JPEG.
# PaddlePaddle (CPU) + PaddleOCR provide PP-Structure layout detection and OCR.
# PP-Structure classifies newspaper page regions (title/text/figure/table)
# and OCRs each region in one pass — handles both English and Indic scripts.
PyMuPDF==1.24.10
paddlepaddle>=2.6.0
paddleocr>=2.8.0
# opendataloader-pdf removed — replaced by PaddleOCR PP-Structure pipeline
importlib_resources==6.4.5

# Sentiment analysis (P17 Signal Room)
vaderSentiment==3.3.2
textblob==0.18.0.post0

# Telegram MTProto user-account client (P17 — read public channels
# without requiring bot membership)
telethon==1.36.0

# Utilities
python-dotenv==1.2.2  # security: CVE-2026-28684
python-jose[cryptography]==3.4.0  # security: PYSEC-2024-232, PYSEC-2024-233

# Transitive-dep security pins — these come from fastapi (starlette),
# reportlab/playwright (pillow), and crawl4ai (litellm). Pinning here
# forces their installed version up regardless of parent constraints.
starlette>=0.47.2  # security: CVE-2024-47874, CVE-2025-54121
pillow>=12.2.0  # security: CVE-2026-25990, CVE-2026-40192, CVE-2026-42308, CVE-2026-42310, CVE-2026-42311
litellm>=1.83.10  # security: CVE-2026-40217, CVE-2026-42203, CVE-2026-42208, CVE-2026-42271
